# dependencies
node_modules/

# runtime data (SQLite DB re-seeds on first boot)
var/

# logs & OS noise
*.log
.DS_Store
npm-debug.log*

# local env
.env
.env.local
dist/

# the app's dependencies (package-lock is committed; node_modules is not)
web/node_modules/
